{
  "name": "@dword-design/base",
  "version": "4.0.1",
  "description": "",
  "keywords": [],
  "repository": "dword-design/base",
  "license": "MIT",
  "author": "Sebastian Landwehr <info@dword-design.de>",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "bin": {
    "base": "./dist/cli.js"
  },
  "scripts": {
    "prepublishOnly": "rimraf dist && babel --out-dir dist --config-file @dword-design/babel-config --copy-files src && node dist/cli.js build",
    "start": "rimraf dist && babel --out-dir dist --config-file @dword-design/babel-config --copy-files src && node dist/cli.js start",
    "test": "rimraf dist && babel --out-dir dist --config-file @dword-design/babel-config --copy-files src && node dist/cli.js test"
  },
  "dependencies": {
    "@babel/cli": "^7.6.4",
    "@babel/core": "^7.6.4",
    "@babel/register": "^7.6.2",
    "@dword-design/base-config": "^1.0.1",
    "@dword-design/base-config-node": "^1.0.0",
    "@dword-design/config-files": "^2.0.0",
    "@dword-design/depgraph": "^1.0.0",
    "@dword-design/functions": "^2.2.2",
    "@dword-design/require-hook-test": "^1.0.0",
    "child-process-promise": "^2.2.1",
    "depcheck": "dword-design/depcheck#fork",
    "fs-extra": "^8.1.0",
    "glob-promise": "^3.4.0",
    "make-cli": "^1.0.9",
    "mocha-per-file": "^2.0.0",
    "nyc": "^14.1.1",
    "pre-commit": "^1.2.2",
    "safe-require": "^1.0.3",
    "wsrun": "^5.2.0"
  },
  "devDependencies": {
    "@dword-design/babel-config": "^1.0.10",
    "expect": "^24.9.0",
    "get-package-name": "^1.0.0",
    "output-files": "^1.1.7",
    "rimraf": "^3.0.0",
    "with-local-tmp-dir": "^2.1.2"
  },
  "baseConfig": {
    "depcheckConfig": {
      "ignoreMatches": [
        "pre-commit"
      ]
    }
  }
}
